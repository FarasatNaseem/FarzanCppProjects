#include <iostream>
#include <math.h>

class SimpleCalculator
{
public:
    float add(float valueOne, float valueTwo);
    float subtract(float valueOne, float valueTwo);
    float mutiply(float valueOne, float valueTwo);
    float divide(float valueOne, float valueTwo);
    float calculatePower(float base, float power);
    float calculateSquareRoot(float value);
    float calculateFactorial(float value);
    float calculateExponential(float power);
};
